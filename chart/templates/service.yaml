apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgmq-postgrest.fullname" . }}
  labels:
    {{- include "pgmq-postgrest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgmq-postgrest.selectorLabels" . | nindent 4 }}
